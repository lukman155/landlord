<%= form_with(model: @room, local: true, url: property_room_path(@room.property, @room)) do |form| %>
  <% if @room.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>
    <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

  <div class="field">
    <%= form.label :room_number %>
    <%= form.number_field :room_number %>
  </div>
  
  <div class="field">
    <%= form.label :rent_amount %>
    <%= form.text_field :rent_amount %>
  </div>
  
  <div class="field">
    <%= form.label :room_type %>
    <%= form.text_field :room_type %>
  </div>
  
  <div class="actions">
    <%= form.submit 'Update Room' %>
  </div>
<% end %>

<%= link_to 'Back', property_rooms_path %>



