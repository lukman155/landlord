<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "Landlord", properties_path, class:'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Properties", properties_path, class:'nav-link' %>
        </li>
        <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to "Edit Profile", users_profile_path, class:'nav-link' %>
        </li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>

      <%= button_to "Sign out", destroy_user_session_path, method: :delete, class:'btn btn-outline-danger' %>

      <% else %>
        
        <%= link_to "Sign in", new_user_session_path, class:'btn btn-outline-primary' %>
        <div class="vr mx-2 mr-2"></div>
        <%= link_to "Sign Up", new_user_registration_path, class:'btn btn-outline-success' %>

      <% end %>
      


    </div>
  </div>
</nav>